const Footer = () => {
  return (
    <footer className="mt-auto px-8 py-3 bg-gradient-to-r from-primary to-secondary">
      <div className="flex items-center justify-center gap-2">
        <span className="text-xs text-primary-foreground/90 italic font-display">
          Driven by Women. Delivered with Excellence.
        </span>
      </div>
    </footer>
  );
};

export default Footer;
